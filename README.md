# Causal Inference Project — GitHub-ready
This repository includes synthetic data generation, IPW estimation, DML, and refutation tests.
Run:
```
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python demo_run.py
```
